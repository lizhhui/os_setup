#!/bin/bash

#git init --bare
#sudo adduser git --no-create-home --disabled-login --shell git-shell
#sudo adduser git --no-create-home --shell git-shell
sudo adduser  --no-create-home --disabled-login git
